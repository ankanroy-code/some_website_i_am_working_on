import { GetStaticProps} from "next";

export const getStaticProps = () : GetStaticProps => {
  return (
  props : { }
  )
}

export const Blog = (props : { }): void => {
  console.log("DEBUG : " + props)
};
